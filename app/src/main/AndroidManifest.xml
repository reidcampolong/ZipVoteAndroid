<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cadb.gov.zipvote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".djd6621" />
        <activity android:name=".IndividualsActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".IndividualActivity" />
        <activity android:name=".GregActivity" />
        <activity android:name=".Greg2Activity" />
        <activity android:name=".John_Candidates_by_color"></activity>
    </application>

</manifest>